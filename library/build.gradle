apply plugin: 'com.android.library'

android {
    compileSdkVersion 26
    buildToolsVersion '26.0.2'

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 26
        versionCode VERSION_CODE.toInteger()
        versionName VERSION_NAME
    }
}

dependencies {
    compileOnly 'com.android.support:support-v4:26.1.0'
    compileOnly 'com.android.support:recyclerview-v7:26.1.0'
}

// New version can be uploaded with './gradlew clean :library:uploadArchives'
apply from: 'gradle-mvn-push.gradle'

// Count methods:
// dx --dex --output=temp.dex classes.jar
// hexdump -s 88 -n 4 -e '1/4 "%d\n"' temp.dex
// Where dx is placed in SDK folder, e.g. ~/Library/Android/sdk/build-tools/26.0.2/
